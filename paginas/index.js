function main() {
  /*  datosCabecera().then((e) => {
    cabecera(e);
    abrirVentana();
  });
  getBienv().then((e) => {
    bienvenida(e);
  });

  pContenido().then((e) => {
    presentacion(e);
  });
  getWorks().then(function (array) {
    for (const w of array) {
      addCard(w);
    }
  });
  formEl();

  getFooter().then((r) => {
    footerEl(r);
  });*/
}
main();
